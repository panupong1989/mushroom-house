# URL ของ backend API (ดู backend/src/index.ts) — ต้องไม่มี / ต่อท้าย
NEXT_PUBLIC_API_URL=http://localhost:3000

# house id ที่จะแสดงผล (ต้องตรงกับ HOUSE_ID ฝั่ง backend / seed.sql)
NEXT_PUBLIC_HOUSE_ID=house-01
